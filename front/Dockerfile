# Use a simple web server to serve static files
FROM nginx:alpine

# Copy the frontend files to the web server's root directory
COPY . /usr/share/nginx/html

# Expose the port the server runs on
EXPOSE 80
